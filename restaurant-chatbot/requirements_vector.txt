# Vector Database Dependencies for Semantic Search
# chromadb is already installed by crewai (~=1.1.0) — do NOT pin a different version
sentence-transformers==2.3.1  # Local embeddings (all-MiniLM-L6-v2, no API costs)
